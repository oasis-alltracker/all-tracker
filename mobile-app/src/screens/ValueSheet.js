import { StyleSheet } from "react-native";
export const ValueSheet = StyleSheet.create({
  /* Fonts */
  primaryFont: {
    fontFamily: "Sego",
  },
  primaryBold: {
    fontFamily: "Sego-Bold",
  },
});
